//Declaração da aplicação
angular.module(
  // module name
  'app', 

  // module dependencies
  [
    'fend.comp.progressbar.loading', 
    'fend.comp.input.utils', 
    'fend.comp.pagination', 
    'ngRoute',
    'ngResource'
  ]
);